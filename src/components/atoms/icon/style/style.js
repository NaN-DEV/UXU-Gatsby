import styled, { css } from "styled-components"

const Icon = styled.div`
  width: auto;
  height: 24px;
  display: block;
  position: relative;
  svg {
    width: auto;
    height: 24px;
    display: block;
    position: relative;
  }
`

export default Icon
